DATABASE_URL=postgres://lahzo:lahzo@localhost:5432/lahzo
DATABASE_POOL_MAX=10

SMS_GATEWAY=mock

# Set SMS_GATEWAY=twilio and fill these values to send real outbound SMS.
TWILIO_ACCOUNT_SID=
TWILIO_API_KEY_SID=
TWILIO_API_KEY_SECRET=

# Optional fallback if you prefer auth token credentials instead of API key credentials.
TWILIO_AUTH_TOKEN=

# Optional. If set, outbound sends use the messaging service instead of the inbound To number as From.
TWILIO_MESSAGING_SERVICE_SID=

SIMULATED_DELAY_MIN_MS=3000
SIMULATED_DELAY_MAX_MS=15000
WORKER_ID=
